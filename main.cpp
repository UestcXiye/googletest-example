#include <iostream>

#include "max.h"

using namespace std;

int main(int argc, char* argv[])
{
    // 使用 max 函数
    int a = 10, b = 20;
    cout << "Max of " << a << " and " << b << " is: " << max(a, b) << endl;

    return 0;
}
